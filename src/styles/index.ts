/**
 * Styles barrel export
 * Import like: import { colors, typography, spacing, animations } from '@/styles';
 */

export * from './tokens';
export { default as tokens } from './tokens';

export * from './animations';
export { default as animations } from './animations';
